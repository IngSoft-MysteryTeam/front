import React from "react-router-dom";

export default function  BotonUnirse () {
    return (
        <div>
            <button className = "btn btn-dark">
            Unirse
            </button>
        </div>
    );
}